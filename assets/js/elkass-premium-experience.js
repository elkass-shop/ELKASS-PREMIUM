(()=>{
 const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
 function progress(){const b=document.createElement('div');b.className='px-scroll-progress';document.body.appendChild(b);const run=()=>{const h=document.documentElement.scrollHeight-innerHeight;b.style.width=(h>0?scrollY/h*100:0)+'%'};addEventListener('scroll',run,{passive:true});run()}
 function reveal(){const nodes=$$('main section, .ef-stats, .ef-brands');nodes.forEach(x=>x.classList.add('px-reveal'));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('px-visible');io.unobserve(e.target)}}),{threshold:.08});nodes.forEach(x=>io.observe(x))}
 function mobile(){if($('.px-mobile-nav'))return;const n=document.createElement('nav');n.className='px-mobile-nav';n.innerHTML='<a href="/"><b>⌂</b>Start</a><a href="/#kategorie"><b>▦</b>Kategorie</a><a href="/app/search/"><b>⌕</b>Szukaj</a><a href="/app/cart/"><b>🛒</b>Koszyk</a><a href="/app/account/"><b>◎</b>Konto</a>';document.body.appendChild(n)}
 function command(){if(!location.pathname.startsWith('/admin'))return;const wrap=document.createElement('div');wrap.className='px-command';wrap.innerHTML='<div class="px-command-box"><input placeholder="Wpisz moduł… (Esc aby zamknąć)"><div class="px-command-results"></div></div>';document.body.appendChild(wrap);const links=$$('aside [data-tab]');const input=$('input',wrap),out=$('.px-command-results',wrap);function render(q=''){out.innerHTML=links.filter(x=>x.textContent.toLowerCase().includes(q.toLowerCase())).map(x=>`<a href="#" data-tab-go="${x.dataset.tab}">${x.textContent.trim()}</a>`).join('');$$('[data-tab-go]',out).forEach(a=>a.onclick=e=>{e.preventDefault();$(`[data-tab="${a.dataset.tabGo}"]`)?.click();wrap.classList.remove('open')})}addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();wrap.classList.add('open');render();setTimeout(()=>input.focus(),20)}if(e.key==='Escape')wrap.classList.remove('open')});wrap.onclick=e=>{if(e.target===wrap)wrap.classList.remove('open')};input.oninput=()=>render(input.value)}
 function tilt(){if(matchMedia('(pointer:fine)').matches===false)return;$$('.ef-category,.ef-product,.ef-review-cards article').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`translateY(-6px) perspective(700px) rotateX(${y*-2}deg) rotateY(${x*2}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}
 document.addEventListener('DOMContentLoaded',()=>{progress();reveal();mobile();command();setTimeout(tilt,700)});
})();
